name: mikepattyn_authress_login
description: Flutter Authress authentication with provider-based guards and GoRouter integration.
version: 0.1.2
homepage: https://github.com/mikepattyn/authress-flutter
repository: https://github.com/mikepattyn/authress-flutter
issue_tracker: https://github.com/mikepattyn/authress-flutter/issues

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.6"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  webview_flutter: ^4.9.0
  url_launcher: ^6.3.1
  shared_preferences: ^2.5.4
  jwt_decode: ^0.3.1
  crypto: ^3.0.5
  go_router: ^17.0.1
  app_links: ^7.0.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.6.1
  build_runner: ^2.10.4
  fake_async: ^1.3.1
  mocktail: ^1.0.4

flutter:
