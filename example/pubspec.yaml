name: mikepattyn_authress_login_example
description: Example app demonstrating mikepattyn_authress_login integration.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.6"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^17.0.1
  app_links: ^7.0.0
  flutter_svg: ^2.2.3
  mikepattyn_authress_login:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/svg/authress.svg
